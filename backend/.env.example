# ============================================
# CONFIGURACIÓN DEL BACKEND - AVATAR COMPLETO
# ============================================
# 
# INSTRUCCIONES:
# 1. Copia este archivo a .env: cp .env.example .env
# 2. Completa los valores con tus credenciales reales
# 3. NUNCA subas el archivo .env a Git (ya está en .gitignore)
#
# ============================================

# ===== SERVIDOR =====
# Puerto en el que correrá el servidor Flask
PORT=8080

# Activar recarga automática en desarrollo (true/false)
# En Windows, se desactiva automáticamente
USE_RELOADER=false

# ===== OPENAI API (OBLIGATORIO) =====
# Obtén tu API key en: https://platform.openai.com/api-keys
# Se usa para el procesamiento de lenguaje natural y análisis de IA
OPENAI_API_KEY=sk-proj-XXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ===== MONGODB =====
# Opción 1: URI completa (recomendado si tienes una URI completa)
# MONGO_URI=mongodb://usuario:password@host:puerto/database?authSource=admin

# Opción 2: Parámetros individuales (usa esta si no tienes URI completa)
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DB=medico_mongo
MONGO_USER=admin
MONGO_PASSWORD=admin123

# ===== API DE PACIENTES =====
# URL del servicio de pacientes (usado para obtener contexto del paciente)
# Si está en el mismo servidor, usa: http://localhost:8003
PATIENT_API=http://localhost:8003
VITE_PATIENT_API=http://localhost:8003
